defaults: &defaults
  email_templates:
    add_cv: "add_cv"
    school_registration: "school_registration"
    club_registration: "club_registration"
    test_results: "test_results"
    blog_subscribe: "blog_subscribe"
    test_results_admin: "test_results_admin"
    school_registration_admin: "school_registration_admin"
    club_registration_admin: "club_registration_admin"



development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults

deployment:
  <<: *defaults
